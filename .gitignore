build/
build_release/
build_debug/
*.nes
*.bin
*.sav
*.log
*.zip
.mcp.json
debug.ini
*.cdl
*.mlbl
*.cht
disasm_ref/
tmp_*.py

# HD texture pack (user-supplied; never committed) + recompiler regen artifacts
hdpack/
*.csv

# nesrecomp is cloned by setup.sh / setup.bat into this directory.
# Pinned SHA recorded in nesrecomp.pin and enforced at CMake configure time.
nesrecomp
nestopia-core

# recomp-ui launcher is a junction to the shared checkout (submodule later).
recomp-ui
build_ui/
